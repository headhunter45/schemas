This project holds schema files intended to be served as a single collection of static files from multiple domains.
It builds as a docker container and uses a simple web server to serve the static content.
